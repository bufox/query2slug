{
	"name": "bufox/query2slug",
	"description": "Map query string parameters to clean, canonical URL slugs.",
	"type": "wordpress-plugin",
	"license": "GPL-2.0-or-later",
	"require-dev": {
		"phpunit/phpunit": "^9.6",
		"yoast/phpunit-polyfills": "^2.0"
	},
	"config": {
		"allow-plugins": {}
	}
}
